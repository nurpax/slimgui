---
url: /recipes.md
---
# Recipes

These recipes are focused, runnable examples of common Dear ImGui patterns as expressed through
Slimgui.

They are organized by task and UI pattern rather than by API symbol, so they work as a practical
companion to the API reference.

The same examples can also be explored interactively:

```bash
python3 example/run_examples.py
```
